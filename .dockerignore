# El Dockerfile hace `COPY . .`. Sin este archivo, al contexto de build entra
# TODO — incluido .git, que tiene el token de GitHub en su config.
.git
.gitignore

node_modules
.next
out
build

# Secretos y config local
.env
.env.*
!.env.example

# Nada de esto se usa en produccion
tests
docs
scripts
db
*.md
tsconfig.tsbuildinfo
npm-debug.log*
.DS_Store
.vscode
.idea
